GophProfile has been deployed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

{{- if .Values.ingress.enabled }}

The API is exposed via Ingress at:
  http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}

If this host doesn't resolve yet, add it to /etc/hosts pointing at your
cluster's ingress IP (`kubectl get ingress {{ include "gophprofile.fullname" . }} -n {{ .Release.Namespace }}`).
{{- else }}

Ingress is disabled. Reach the API with:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "gophprofile.fullname" . }}-{{ .Values.server.name }} 8080:{{ .Values.server.service.port }}
{{- end }}

Check rollout status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if .Values.migrationHook.enabled }}

Database migrations run automatically as a Helm hook Job before pods roll
out on install/upgrade. Check its logs with:
  kubectl logs -n {{ .Release.Namespace }} job/{{ include "gophprofile.fullname" . }}-migrate-{{ .Release.Revision }}
{{- end }}

{{- if not .Values.serviceMonitor.enabled }}

Note: serviceMonitor.enabled is false, so Prometheus will not automatically
scrape this release. Set it to true once the Prometheus Operator CRDs
(e.g. via kube-prometheus-stack) are installed in the cluster.
{{- end }}
